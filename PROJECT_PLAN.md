# 安巡智脑第一版项目计划

## 1. 项目目标

建设一个可运行、可部署、可演示的校园交通安全巡检智能体。系统接收校园道路图片和地点信息，通过 LangGraph 状态图依次完成输入校验、图片质量检查、YOLO 交通标志检测、知识库检索、风险研判、整改建议、报告生成、结果保存和人工复核分流。

## 2. 统一技术路线

- 前端：Vue 3、Vite、TypeScript、Element Plus、Pinia、Vue Router、Axios。
- 后端：Python 3.10+、FastAPI、Pydantic、SQLAlchemy、Alembic。
- 智能体：开源 LangGraph `StateGraph`，明确节点、状态和条件边。
- 视觉工具：Ultralytics YOLO26；在 TT100K 上重新训练，旧 YOLO11 权重仅用于基线对照。
- 知识库：Qdrant；开发环境支持同一官方客户端的嵌入式模式。
- 大模型：OpenAI Chat Completions 兼容接口，通过环境变量切换供应商。
- 数据库：生产使用 MySQL 8；本地测试允许 SQLite。
- 部署：Docker Compose + Nginx。

## 3. 参考资料冲突处理

三份 Word 文档保留了 SF-FastGPT、FastGPT 或深信服平台路线，这些内容仅作为早期需求参考。最新总控要求优先：本项目不依赖主办方平台，知识检索、工作流、工具调用和异常分支全部在自研系统中实现。

旧项目不直接修改。仅复用已训练权重、权重内置类别、真实检测样例及成熟推理思路，不复制桌面 UI、旧 Flask 页面或旧平台配置。

## 4. 第一版 MVP

1. 创建巡检任务并上传图片。
2. 通过真实 LangGraph 工作流执行全部节点。
3. YOLO 模型单例加载并输出真实框、类别、置信度和耗时。
4. 图片模糊、亮度、尺寸和损坏检查。
5. PDF、DOCX、TXT、Markdown 知识文档入库与 Top-K 检索。
6. 规则 + RAG + 可配置 LLM 的结构化风险研判。
7. HTML 巡检报告、历史记录、执行轨迹和人工复核。
8. SSE 展示真实步骤状态。
9. Vue 管理端覆盖仪表盘、新建巡检、详情、历史、复核、知识库和设置。
10. MySQL、Qdrant、前后端与 Nginx 的容器编排。

## 5. 比赛黄金演示链路

首页 → 新建巡检 → 选择校门口 → 上传真实图片 → 启动智能巡检 → 实时展示 Agent 节点 → 展示 YOLO 证据 → 展示知识依据 → 展示风险与整改建议 → 查看报告 → 在历史任务中复现记录。

异常演示至少覆盖模糊图片、未检出目标、低置信度、知识依据不足、LLM 失败和 YOLO 失败；系统不得把失败显示为成功。

## 6. 真实应用验证

系统记录任务总耗时、YOLO/知识检索/Agent 耗时、工具调用状态、人工复核原因、报告生成状态、人工修改内容和用户反馈。所有比赛指标只有在真实校园样本测试后才能填写，计划目标不得写成已实现结果。

## 7. 外部资源边界

- 当前已有可验证权重为 45 类 TT100K YOLO11 权重，只作为对照基线。最终主模型明确采用 YOLO26：先运行 YOLO26m 调试/基线，再以 YOLO26l 作为精度优先候选；训练完成后由 `yolo26-tt100k-best.pt` 接入系统。
- LLM API Key、生产 MySQL、生产 Qdrant 和真实校园图片由部署/测试时配置。
- 未配置 LLM 时仅启用明确标记的规则保守模式，并自动进入人工复核，不伪造大模型调用。
