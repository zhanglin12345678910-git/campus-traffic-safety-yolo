# 系统迭代与对比实验开发总结报告

**项目名称：** YOLO11 交通标志小目标检测系统与消融实验补充  
**项目路径：** `<LOCAL_PATH>`  
**整理日期：** 2026年5月26日  

---

## 1. 工作背景

本轮工作主要围绕论文盲审意见进行补充。盲审意见认为现有方法中 PSSM、C2PSA-DA、WIoUv3 等模块容易被认为是已有技术组合，建议进一步补充：

1. 不同多尺度特征提取模块的对比实验，用于说明 PSSM 在交通标志小目标检测任务中的优势；
2. 系统章节的差异化功能，避免系统只停留在图片、视频、摄像头检测的基础展示层面；
3. 增加检测结果统计、模型性能在线对比、批量检测报告等更适合写入硕士论文第五章的工程功能。

因此，本轮开发分为两条线：实验配置补充和检测系统迭代开发。

---

## 2. 多尺度特征提取模块对比实验补充

### 2.1 新增模块目的

为了和论文中的 PSSM 模块进行同类对比，新增了 3 类多尺度特征提取模块：

| 模块名称 | 论文表格建议命名 | 对比意义 |
|---|---|---|
| `C2f_SPPFEnhance` | YOLO11n+SPPF | 常见池化上下文增强模块 |
| `C2f_ASPP` | YOLO11n+ASPP | 经典空洞卷积多尺度上下文模块 |
| `C2f_MSConv` | YOLO11n+MSConv | 普通并行多尺度卷积模块 |

这三组模型只用于和 C2f-PSSM 进行多尺度特征提取能力对比，不引入 DA 注意力、P2 检测头、WIoU、SlimNeck、剪枝或蒸馏等额外因素，保证实验变量更单一。

### 2.2 新增模块位置

模块定义位置：

```text
ultralytics/nn/extra_modules/block.py
```

关键类：

```text
SPPFEnhanceBlock
ASPPBlock
MSConvBlock
C2f_SPPFEnhance
C2f_ASPP
C2f_MSConv
```

框架注册位置：

```text
ultralytics/nn/tasks.py
```

已将三个模块加入 YOLO 的 YAML 解析流程，可以直接在模型配置文件中调用。

### 2.3 新增 YAML 文件

文件统一放在：

```text
PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/
```

包含：

```text
yolo11n-C2f-SPPFEnhance.yaml
yolo11n-C2f-ASPP.yaml
yolo11n-C2f-MSConv.yaml
README.md
```

### 2.4 模型构建验证

三个模型均已完成 YOLO 构建检查，能够正常解析 YAML、生成 summary，并显示参数量和 FLOPs。

| 模型 | 参数量 | FLOPs |
|---|---:|---:|
| YOLO11n+SPPF | 约 2.27M | 约 6.5G |
| YOLO11n+ASPP | 约 2.55M | 约 7.5G |
| YOLO11n+MSConv | 约 3.59M | 约 11.1G |

### 2.5 训练命令示例

```bash
yolo detect train model=PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/yolo11n-C2f-SPPFEnhance.yaml data=xxx.yaml imgsz=640 epochs=300 batch=32
```

```bash
yolo detect train model=PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/yolo11n-C2f-ASPP.yaml data=xxx.yaml imgsz=640 epochs=300 batch=32
```

```bash
yolo detect train model=PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/yolo11n-C2f-MSConv.yaml data=xxx.yaml imgsz=640 epochs=300 batch=32
```

---

## 3. 交通标志检测系统迭代开发

系统迭代遵循“不直接破坏原始文件、每一轮生成新文件”的方式完成。

| 文件 | 作用 | 迭代说明 |
|---|---|---|
| `app.py` | 原始系统入口 | 保留原有功能，并按要求将推理设备改为 GPU |
| `app_iter1_statistics.py` | 第一轮迭代 | 新增检测统计分析与 CSV 导出 |
| `app_iter2_compare.py` | 第二轮迭代 | 在第一轮基础上新增模型性能在线对比 |
| `app_iter3_batch.py` | 第三轮迭代 | 在第二轮基础上新增批量图片检测与报告导出 |

---

## 4. 第一轮：检测结果统计分析

### 4.1 新增核心类

```text
DetectionStats
```

该类集中管理检测统计逻辑，避免把统计代码堆叠在界面刷新函数中。

### 4.2 新增统计指标

系统在图片、视频、摄像头检测中可统计：

1. 当前模型名称；
2. 当前输入类型；
3. 当前检测目标总数；
4. 当前画面目标数；
5. 各类别出现次数；
6. 最高置信度；
7. 平均置信度；
8. 单次推理时间；
9. 累计平均推理时间；
10. FPS；
11. 累计处理帧数；
12. 累计检测到目标的帧数。

### 4.3 新增界面功能

右侧信息面板新增“检测统计分析”区域，显示：

```text
平均用时
FPS
累计目标数
检测帧数
平均置信度
类别统计摘要
```

新增按钮：

```text
📊 导出统计报告
```

### 4.4 CSV 导出内容

导出的 CSV 使用 `utf-8-sig` 编码，包含总体统计和逐目标明细。

总体统计字段包括：

```text
model_name
source_type
total_frames
detection_frames
total_objects
avg_confidence
max_confidence
avg_inference_ms
avg_fps
```

逐目标字段包括：

```text
frame_index
image_name/source_name
model_name
class_name
confidence
x1
y1
x2
y2
inference_ms
```

---

## 5. 第二轮：模型性能在线对比

### 5.1 功能目标

该功能用于同一张图片下两个模型的检测效果和推理性能对比，适合论文第五章展示不同模型在相同输入条件下的差异。

### 5.2 新增界面组件

新增：

```text
对比模型A
对比模型B
⚖️ 模型对比
保存对比结果
```

两个按钮已根据界面显示问题进行紧凑化调整，避免文字底部显示不全，并使按钮尺寸和上方模型下拉框更加协调。

### 5.3 对比指标

每个模型显示：

```text
模型名称
推理时间 ms
FPS
检测目标数
平均置信度
最高置信度
主类别
参数量
FLOPs
```

参数量和 FLOPs 当前作为预留字段显示。

### 5.4 保存内容

支持保存：

1. 左右拼接后的对比检测图片；
2. 对比结果 CSV。

CSV 字段包括：

```text
image_name
model_a
model_b
model_a_time_ms
model_b_time_ms
model_a_fps
model_b_fps
model_a_object_count
model_b_object_count
model_a_avg_conf
model_b_avg_conf
```

---

## 6. 第三轮：批量图片检测与报告导出

### 6.1 功能目标

第三轮在第二轮基础上新增批量图片检测，用于对文件夹中的多张交通标志图片进行统一推理、保存检测结果图并导出完整 CSV 报告。

### 6.2 新增核心类

```text
BatchWorker(QThread)
```

批量检测使用 QThread 执行，避免界面在批量推理过程中卡死。

### 6.3 新增按钮与控件

```text
📁 批量图片检测
⏸ 停止批量
批量检测进度条
```

### 6.4 批量检测流程

1. 用户选择图片文件夹；
2. 系统读取 `jpg/jpeg/png/bmp` 图片；
3. 逐张图片进行 YOLO 检测；
4. 实时刷新当前检测图、检测信息、统计面板和进度条；
5. 在输入文件夹下生成 `batch_results_时间戳` 输出目录；
6. 保存每张检测结果图；
7. 导出完整 `detection_report.csv`。

### 6.5 批量报告特点

当某张图片没有检测目标时，也会写入一行记录，避免报告中漏掉无目标图片。单张图片处理失败时写入 `ERROR` 行，便于后续排查。

---

## 7. GPU 推理设置

按照要求，`app.py` 和三个迭代版本均设置了统一推理设备变量：

```python
INFER_DEVICE = "0"
```

各处 `model.predict()` 调用统一使用：

```python
device=INFER_DEVICE
```

这样运行界面时默认使用第 0 块 GPU 推理。需要注意，如果训练任务正在占用 GPU，同时运行界面推理会共同占用显存和算力，可能导致训练速度下降或显存不足。

---

## 8. 运行方式

运行第一轮统计版：

```bash
python app_iter1_statistics.py
```

运行第二轮对比版：

```bash
python app_iter2_compare.py
```

运行第三轮批量版：

```bash
python app_iter3_batch.py
```

建议论文展示和最终系统演示优先使用：

```bash
python app_iter3_batch.py
```

因为第三轮版本包含前两轮功能。

---

## 9. 验证情况

已完成以下验证：

1. 三个多尺度对比 YAML 可以被 YOLO 正常解析；
2. 三个模型可以正常构建并显示参数量、FLOPs；
3. `app_iter1_statistics.py` 通过 Python 编译检查；
4. `app_iter2_compare.py` 通过 Python 编译检查；
5. `app_iter3_batch.py` 通过 Python 编译检查；
6. 三个迭代版均保留原始图片、视频、摄像头检测、多模型选择、检测结果保存和清除显示功能。

---

## 10. 论文写作建议

第五章系统设计可以从以下角度展开：

1. 系统整体架构：模型加载层、输入处理层、推理检测层、结果可视化层、统计分析层；
2. 多线程检测机制：视频、摄像头、批量检测均通过 QThread 保证界面响应；
3. 检测统计分析模块：支持类别分布、目标数量、置信度、推理速度和 FPS 统计；
4. 检测报告导出模块：支持 CSV 格式保存总体统计和逐目标明细；
5. 模型性能在线对比模块：支持同一输入下对不同模型进行检测效果和推理性能对比；
6. 批量图片检测模块：支持文件夹级检测、结果图保存、完整报告导出和进度显示；
7. 工程可用性：支持图片、视频、摄像头、批量图片四类输入，增强系统完整性和实用价值。

---

## 11. 本轮新增和修改文件汇总

### 11.1 新增文件

```text
app_iter1_statistics.py
app_iter2_compare.py
app_iter3_batch.py
系统迭代与对比实验开发总结报告.md
PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/README.md
PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/yolo11n-C2f-SPPFEnhance.yaml
PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/yolo11n-C2f-ASPP.yaml
PMSFA_Ablation_Study_20260524/MultiScale_Module_Comparison/yolo11n-C2f-MSConv.yaml
```

### 11.2 修改文件

```text
app.py
ultralytics/nn/extra_modules/block.py
ultralytics/nn/tasks.py
PMSFA_Ablation_Study_20260524/📖_中文文档索引.md
📋_项目文件清单.md
```

---

## 12. 总结

本轮工作完成了论文实验与系统功能两个方向的增强。实验部分补充了 SPPF、ASPP、MSConv 三类同类多尺度特征提取模块，为 PSSM 的对比论证提供了更直接的实验支撑；系统部分完成了统计分析、CSV 导出、模型在线对比、批量检测和报告导出等功能，使交通标志检测系统从基础演示型系统提升为具备分析能力、对比能力和批处理能力的完整应用系统。

这些内容可以分别支撑论文第四章的对比实验和第五章的系统设计与实现。
