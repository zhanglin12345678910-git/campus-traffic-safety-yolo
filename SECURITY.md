# 安全与隐私

## 配置与凭据

真实 `.env`、服务密钥、数据库密码、证书和访问令牌不得提交。仓库仅提供空值配置模板。服务密钥只保存在后端环境中，不嵌入前端源码、构建文件、截图或日志。

部署前设置独立的数据库密码、访问密钥和允许的来源地址，不使用示例密码。基础认证不提供传输加密；需要远程访问时应配置 HTTPS 和适当的访问控制，不直接暴露数据库或内部服务。

## 资料与运行数据

原始校园素材、上传文件、输出报告、数据库、运行日志和个人配置不属于仓库内容。使用现场资料前取得授权，按需移除或遮挡人脸、车牌、姓名、电话和精确定位，并核实图片元数据。

外部视觉服务涉及资料传输。默认模板关闭该功能；启用前确认授权、数据范围和提供方政策。检测到人员的图片使用隐私保护回退，但检测不能保证识别所有个人信息，仍需在上传前检查素材。

## 泄露处理

如果凭据曾被提交，先撤销或轮换凭据；从当前文件删除不等于从 Git 历史中移除。涉及历史清理时应先确认影响范围并协调已有克隆。

提交问题或日志时提供脱敏后的最小复现信息，不公开真实账号、密钥或个人资料。
