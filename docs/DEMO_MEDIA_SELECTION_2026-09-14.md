# 校园实拍演示素材清单（2026-09-14）

新增目录 `校园照片/新增照片和视频/MVIMG_20260913_190642_2等91项文件/` 实际包含 78 张 JPG、13 个 MP4。未移动、删除或修改原始素材；这批素材用于应用验证，不用于恢复训练。

## 已验证的照片

| 文件 | 建议演示用途 | 本轮真实结果 |
|---|---|---|
| `MVIMG_20260913_184554.jpg` | 校门口人员/非机动车检测、证据到报告及转人工闭环 | 3 个目标、1 个 person；DeepSeek `analysis_mode=llm`、medium，进入 review，报告可读 |
| `MVIMG_20260913_191815.jpg` | 人车混合场景、人员聚集候选、隐私闸门 | 13 个目标、12 个 person；触发 8 人聚集阈值，review，报告可读；另一次 VLM 回归确认人物闸门零新增视觉调用 |
| 旧素材 `校园照片/新增照片/车/IMG_9759.JPG` | 千问视觉证据的待复验备用样例 | 本轮云端视觉调用 WinError 10013，未取得视觉结果，任务降级为 review；不能用本轮结果宣称 qwen 联网成功 |

前两张建议作为本次演示首选：有可见人物/车辆、能展示真实检测和真实转人工。公开演示前须核对授权并脱敏可辨识人脸、车牌；自动隐私闸门不等于完整脱敏保证。

## 视频：已验证能力，但不是逆行真值样例

`VID_20260913_185218.mp4`：2160×3840、约 60 fps、271 源帧；现有 API 实际成功分析 46 帧，stride=6，等比分析分辨率 540×960，耗时 76,592.17 ms，最大同帧 12 人、27 个人员轨迹。

可演示上传、自动缩放采样、ByteTrack、真实聚集规则输出。不适合用来证明真实逆行：镜头有移动，本轮 `left_to_right` 是接口配置测试而非校园真实方向，返回的 7 条反向轨迹只说明规则被触发。应优先拍摄固定机位、明确通行方向的视频完成方向真值验证；其余 12 个新增视频本轮尚未逐一筛完，不能称整个视频素材集已验收。

## 复现与证据

本轮运行 `scripts/verify_delivery_media.py`，使用真实 API、正式权重、生产质量阈值、隔离 SQLite/Qdrant/上传/输出目录；导入现有 5 份来源，不导入示例夹具，不写生产任务库。

结果：`outputs/delivery-media-20260914.json`。隔离运行目录随测试结束回收，JSON 中临时视频 URL 不是长期演示地址；演示时从上述原始文件重新上传。返回成功不等于所有视频播放器/服务器性能均已验收。

测试方向不可冒充实际交通方向，聚集候选不可冒充已成立违法事实。训练保持 32/80，不恢复。
