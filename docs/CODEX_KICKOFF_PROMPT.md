# Codex 开工提示词（现状认知轮 · 2026-09-14）

> **用法**：把下面横线以内的全部内容，原样粘贴给 Codex 作为第一条消息。
> **本轮目的**：只让 Codex 读文档、建立现状认知并交回理解报告，**不写任何代码、不改任何文件**。
> 报告确认后，再发第二条指令谈干什么。
> **最后同步**：2026-09-14 02:10（VLM 接入 + qwen3-vl-plus 启用轮之后）。

---

你是接手「安巡智脑」项目的工程师。项目此前由多个 AI 协作推进，**全部背景、决策依据、踩坑记录都已写成文档**——不需要我重新讲一遍项目。

本轮你的唯一任务是：**读文档，建立准确的现状认知，然后交回一份理解报告。不写任何代码、不改任何文件、不重启任何服务。**

## 项目路径

`<LOCAL_PATH>`
（独立 git 仓库，只在它内部操作；当前有大量未提交的有效改动，**禁止 git reset/checkout/clean 或任何覆盖性操作**。）

## 要读的文档（按顺序，不要跳读）

1. **`docs/HANDOFF.md`** —— 主交接文档，按时间倒序排列，**最上面两节是当前有效状态**：
   - 顶部「2026-09-13 深夜 · VLM 接入轮」：VLM 旁挂视觉研判（模式乙）已接入、已默认启用、提供方为 qwen3-vl-plus（含 E/E2 节两次用户拍板记录与 G 节运行态快照）。
   - 第二节「2026-09-14 凌晨 · 知识库轮」：知识库三项决策（A1/B1/C3）与双维度审计结论。
   - 再往下的轮次是历史脉络，用来理解"为什么变成现在这样"，不要当当前状态。
2. **`docs/TEST_REPORT.md` 第 24、25 节** —— 最近两轮的实测矩阵与如实记录的边界。
3. **`README.md` + `PROJECT_PLAN.md`** —— 项目定位与计划基线。
4. **`frontend/DESIGN.md`** —— 前端设计唯一事实来源（当前 v7，GitHub 灰黑科技风，**不得改回蓝色系**）。
5. **`docs/VLM_INTEGRATION_PROPOSAL.md` 第 7 节** —— VLM 方案的评审结论（模式乙的三条铁律从这里来）。

## 读完必须内化的硬约束（文档里都有出处，这里只点题）

- 解释器用 conda `yolo_change`；后端 CWD 必须是 `backend/`；从仓库根跑 backend 代码时 YOLO 权重路径要给绝对路径。
- 训练已锁定（32/80），不恢复、不改称完整训练。
- 真实性铁律：无接口支撑的数字只显示「—/未接入」。
- 知识库只用国家法律法规+强制性国标并标条款号，绝不编造校规。
- 密钥只在 `.env`（已被 .gitignore 忽略），不进代码/日志/报告/前端。
- 生产库任务表是修复前基线数据，效果验证一律走隔离库。
- 每轮改完必须同步四件套：HANDOFF 顶部新轮次、TEST_REPORT 新一节、当日日志、长期记忆文件。

## 交回的理解报告（不超过一页）

1. 用 5~8 句话讲清这个系统现在是什么、主链路怎么跑。
2. 当前运行态：哪些服务在跑、VLM 是什么角色、哪条铁律最容易被误解。
3. 你认为的未完成事项清单（只列文档里写明的，不要自己发明新需求）。
4. 你发现的文档之间任何相互矛盾之处（如有）。

报告交给我确认。我没有发第二条指令之前，**不要动手改任何东西**。
