# Codex 知识库任务提示词（决策 + 执行轮）

> **用法**：把下面横线以内的全部内容，原样粘贴给 Codex 作为第一条消息。
> **与前一份提示词的区别**：`CODEX_KICKOFF_PROMPT.md` 是「现状认知轮」（不写代码）；
> 本份是「知识库决策 + 执行轮」——**Codex 拥有决策权，用户不拍板**。
> **最后同步**：2026-09-13。

---

你是接手「安巡智脑」项目的工程师。项目背景、踩坑记录、当前状态都已写成文档，不需要我重新讲。

本轮任务：**补齐知识库缺口，并且由你自己做出该做的决策 —— 用户不参与拍板，但要求你的决策有依据、有记录。**

## 第一步：读这两份（必读，不要跳读）

1. **`docs/KNOWLEDGE_GAP_ASSESSMENT_2026-09-13.md`** —— 本轮任务书。
   里面有实测证据（11 个真实查询的召回数据）、三个待决策项、缺口清单、硬约束、验收要求。
   **你的决策就基于这份文档给出的事实。**
2. **`docs/HANDOFF.md`** —— 交接主文档，**只读「最新补充」「最新一轮」两节**即可建立现状认知
   （再往下的轮次是历史脉络，用来理解「为什么会变成现在这样」，**不要当当前状态**）。

## 第二步：读写作规范（动手前必读）

用户级 skill **`rag-knowledge-base-authoring`**（路径 `<LOCAL_PATH>`）。
它固化了这个项目的 RAG 写作硬规则与踩过的坑。**本次诊断又发现两条新坑，已回写进该 skill，一并读。**

## 第三步：你得自己拍三件事（不要回来问我）

详见表述与选项见任务书「三、待你决策的三件事」：

- **决策 A：知识库业务边界划到哪**（建议维持「只做视觉可验证的隐患」，但你可以推翻，要说明理由）
- **决策 B：逆行闭环要不要打通**（现状：逆行判定已实现但只在视频路径、且结果不入库）
- **决策 C：检索技术路线**（保持 hash 重写文档 / 换语义嵌入 / 分两步）

**判据**：任何决策都必须能回答「**证据从哪来**」。
如果某类知识检不出证据，补进库只会变成第二个「逆行」——文档躺着、报告引不到、还挤占召回席位。

**决策必须写进 `docs/HANDOFF.md`：选了什么、为什么、放弃了什么、有什么代价。**

## 第四步：执行（按你自己定的优先级）

任务书「四、已确认的缺口清单」里列了 7 项，**你可以调整优先级，但要说明取舍**。

无论最终做哪几项，都请遵守：

- **只补「查得到出处」的内容。** 编造校规是本项目最严重的违规——报告是要交给学校看的。
  只用国家法律 / 行政法规 / 部门规章 / 强制性国家标准，**每条标注全称与条款号，条款号必须逐条核实**。
- **不要动生产库历史任务**（`backend/data/campus_safety.db` 是修复前基线）。知识库改动用隔离库验证。
- 替换已入库文档走**「删旧 → 传新」**，不要重复上传同名文档。
- 入库前跑 `scripts/check_knowledge_chunks.py`，确认 0 个表格残块。
- **不要删任何文件**，除非走完「grep 全仓路径引用 → 查 DB `stored_path` → 确认可从留存副本复原」三步。
- 不要向生产知识库上传演示文档。

## 第五步：验证并交付

1. **扩样检索审计**：现有 `scripts/verify_knowledge_recall.py` 只覆盖 8 个区域类型，
   请加上**「隐患类型」维度**（逆行、违停、占用消防通道、非机动车乱停、恶劣天气……），
   产出「改进前 / 改进后」对照表：**零命中数 / top5 席位分布 / 分数区间**。
2. **零退化验证**：改完复跑审计，三项指标与改前逐位对比。
3. **真实任务回归**：挑 1 个真实任务重跑，确认报告「知识依据」引用了新文档。
   ⚠️ **不要用单次重跑判断改动效果** —— 判定链路存在残余非确定性，`risk_level` 翻转是低概率非必然。
4. **后端全量测试必须过**：
   `cd backend && <LOCAL_PATH> -m pytest -q -p no:cacheprovider --basetemp=<全新唯一路径>`
   期望 **64 passed**。
   （`--basetemp` **必须用全新唯一路径**，复用会被残留撑满、表现为几十个 errors —— **是路径复用不是代码回归**。）
5. **写回交接**：更新 `docs/HANDOFF.md`（滚动交接，**新内容加在最上面、不删历史轮次**），
   并在 `.workbuddy/memory/` 当日日志里记一笔。

## 环境事实（省得试错）

- **Python 解释器** = `<LOCAL_PATH>`。
  Git Bash 里裸敲 `python` 会命中**没装 pytest** 的托管 3.13，报 `No module named pytest`。
- 后端必须以 **`backend/` 为 CWD** 启动，否则 `data/campus_safety.db`、`data/qdrant`、`../uploads` 全指错地方。
- 后端若没起：`<解释器> -m uvicorn app.main:app --host 127.0.0.1 --port 8000`
- 检索接口 `POST /api/v1/knowledge/search` 无需鉴权；`POST/DELETE` 带 `require_api_key`、`GET` 不带。
- 线上知识库现状 **5 份 / 63 块**；源目录 `knowledge/` 是 6 个文件 / 64 块，
  多的 1 块是**未入库**的《校园交通巡检示例规范》夹具，**别误判成没删干净**。

## 交付口径

最后给我一份简报（**能直接看的短结论，不要长文档**）：

- 三个决策各选了什么、一句话理由
- 实际补了哪几份文档、各多少块
- 检索审计「改前 / 改后」对照数字
- 测试结果
- 有哪些事你**判断为不该做**、为什么（这一条很重要，不要省略）
- 遗留与风险
