# 智能交通标志检测系统 - Web端架构图

## 系统层级结构（文字版）

```
┌─────────────────────────────────────────────────────────────┐
│                    第1层：用户层                             │
│                  用户浏览器 (User)                           │
└─────────────────────────────┬───────────────────────────────┘
                              │ ↓ (用户请求)
┌─────────────────────────────────────────────────────────────┐
│                  第2层：前端交互层                           │
│          HTML + CSS + JavaScript (Web Interface)             │
└─────────────────────────────┬───────────────────────────────┘
                              │ ↓ (HTTP请求)
┌─────────────────────────────────────────────────────────────┐
│              第3层：Flask API接口层                          │
├──────────────────────────────────────────────────────────────┤
│  A1: 图片检测              A4: 模型管理                      │
│  A2: 视频检测              A5: 历史查询                      │
│  A3: 摄像头检测            A6: 统计分析                      │
└──────┬──────────┬──────────┬──────────┬──────────┬──────────┘
       │          │          │          │          │
       ↓ A1       ↓ A2       ↓ A3       ↓ A4       ├─→ A5,A6
┌──────────────────────────────────────────────┐  │
│      第4层：核心业务层                        │  │
├──────────────────────────────────────────────┤  │
│  Engine: 检测引擎                            │  │
│  ModelMgr: 模型管理器                        │  │
│  ResultProc: 结果处理                        │  │
└──┬────────────┬──────────────┬──────────────┘  │
   │            │              │                 │
   ├─→ ModelMgr ├─→ YOLO       ├─→ DB ←─────────┘
   │            │              │
   └────────────┴──────────────┴──→ FS (文件存储)
                │
                ↓
┌──────────────────────────────────────────────────────────────┐
│          第5层：YOLO推理层                                   │
│      Ultralytics YOLO (GPU/CPU推理)                          │
└──────────────────┬───────────────────────────────────────────┘
                   │ ↓ (检测结果)
                   │
              ResultProc (第4层)
                   │ ↓
                  Web (第2层)
                   │ ↓ (返回JSON+图片)
                 User (第1层)
                
┌──────────────────────────────────────────────────────────────┐
│          第6层：数据持久层                                   │
├──────────────────────────────────────────────────────────────┤
│  DB: SQLite数据库 (存储检测记录)                             │
│  FS: 文件存储 (uploads/ + results/)                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 完整数据流向表

| 来源 | 指向 | 说明 | 代码位置 |
|------|------|------|----------|
| **用户浏览器** | → 前端Web | 用户交互请求 | - |
| **前端Web** | → API接口 | 发送HTTP请求 | JavaScript |
| **A1 图片检测** | → 检测引擎 | 调用图片处理 | `/api/detect/image` |
| **A2 视频检测** | → 检测引擎 | 调用视频处理 | `/api/detect/video` |
| **A3 摄像头检测** | → 检测引擎 | 调用摄像头处理 | `/api/camera/frame` |
| **A4 模型管理** | → 模型管理器 | 切换模型 | `/api/models/active` |
| **A5 历史查询** | → 数据库 | 查询记录 | `/api/history` |
| **A6 统计分析** | → 数据库 | 统计数据 | `/api/stats` |
| **检测引擎** | → 模型管理器 | 请求模型 | `get_model()` |
| **检测引擎** | → 数据库 | 保存检测记录 | `db.session.add()` |
| **检测引擎** | → 文件存储 | 保存上传和结果 | `save_result_image()` |
| **模型管理器** | → YOLO推理 | 加载和调用模型 | `YOLO()` |
| **YOLO推理** | → 结果处理 | 返回检测结果 | `model.predict()` |
| **结果处理** | → 前端Web | 返回JSON+Base64 | `jsonify()` |
| **前端Web** | → 用户浏览器 | 显示检测结果 | 浏览器渲染 |

---

## 核心模块对照表

| 架构层 | 模块名称 | 代码位置 | 主要功能 |
|--------|----------|----------|----------|
| **第3层** | API接口 | `@app.route` 装饰器 | 接收HTTP请求，返回JSON |
| **第4层** | 检测引擎 | `detect_image()`, `VideoProcessor`, `get_camera_frame()` | 处理不同来源的检测任务 |
| **第4层** | 模型管理器 | `MODELS`, `get_model()`, `models_lock` | 多模型配置、懒加载、线程安全 |
| **第4层** | 结果处理 | `process_detection_result()`, `save_result_image()` | 解析检测框、绘制可视化 |
| **第5层** | YOLO推理 | `model.predict()` (Ultralytics) | GPU/CPU推理执行 |
| **第6层** | 数据库 | `Detection` 模型, `db.session` | SQLite存储检测历史 |
| **第6层** | 文件存储 | `uploads/`, `results/` | 保存用户文件和结果 |

---

## 12个API接口完整列表

| 接口路径 | HTTP方法 | 功能描述 |
|----------|---------|---------|
| `/api/detect/image` | POST | 上传图片进行目标检测 |
| `/api/detect/video` | POST | 上传视频进行目标检测 |
| `/api/video/frame` | GET | 获取视频检测的实时帧 |
| `/api/video/stop` | POST | 停止视频处理 |
| `/api/camera/start` | POST | 启动摄像头检测 |
| `/api/camera/frame` | GET | 获取摄像头检测的实时帧 |
| `/api/camera/stop` | POST | 停止摄像头检测 |
| `/api/models` | GET | 获取可用模型列表 |
| `/api/models/active` | GET/POST | 获取/切换当前活跃模型 |
| `/api/history` | GET | 查询检测历史记录 |
| `/api/stats` | GET | 获取统计分析数据 |
| `/api/health` | GET | 系统健康检查 |

---

## 关键特性说明

### 1. 多模型支持
- **PSSM-YOLO** (自研改进模型)
- YOLOv11n, YOLOv10n, YOLOv9t, YOLOv8n 等
- 支持运行时热切换模型

### 2. 三种检测模式
- **图片检测**: 单张上传，同步返回
- **视频检测**: VideoProcessor 多线程异步处理
- **摄像头检测**: 实时流处理，按时间间隔保存

### 3. 性能优化
- 模型懒加载: 首次使用才加载到内存
- 线程安全锁: 防止并发加载冲突
- 推理参数可调: conf/iou/imgsz 灵活控制

### 4. 数据管理
- SQLite 轻量级数据库
- Detection 表记录每次检测详情
- 文件系统管理上传和结果

