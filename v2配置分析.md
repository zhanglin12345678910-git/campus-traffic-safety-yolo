# v2 配置特征图尺寸分析

## 输入: 640x640

### Backbone 特征图尺寸流动

```
Layer 0: Conv[64, 3, 2]     输入:640x640x3   → 输出:320x320x64   (P1/2)
Layer 1: Conv[128, 3, 2]    输入:320x320x64  → 输出:160x160x128  (P2/4)
Layer 2: CSP_PMSFA[128]     输入:160x160x128 → 输出:160x160x128  ✅
Layer 3: Conv[256, 3, 2]    输入:160x160x128 → 输出:80x80x256    (P3/8)
Layer 4: CSP_PMSFA[256]     输入:80x80x256   → 输出:80x80x256    ✅
Layer 5: Conv[512, 3, 2]    输入:80x80x256   → 输出:40x40x512    (P4/16)
Layer 6: CSP_PMSFA[512]     输入:40x40x512   → 输出:40x40x512    ✅
Layer 7: SPPF[512]          输入:40x40x512   → 输出:40x40x512    ✅
Layer 8: C2DA[512]          输入:40x40x512   → 输出:40x40x512    ✅
```

### Head 特征图尺寸流动

```
Layer 9:  Upsample           输入:40x40x512   → 输出:80x80x512
Layer 10: Concat[-1, 6]      输入:80x80x512 + 40x40x512  ← ❌ 尺寸不匹配！
```

## ❌ 问题发现！

**Layer 10 的 Concat 操作**：
- 输入1: Layer 9 (Upsample后) = **80x80** x 512
- 输入2: Layer 6 (Backbone P4) = **40x40** x 512

**尺寸不匹配！** 80 vs 40

## 🔧 原因分析

Layer 9 是从 Layer 8 (C2DA, 40x40) 上采样2倍，变成 80x80
但 Layer 6 (Backbone P4) 是 40x40

**需要 Concat 的两个特征图尺寸必须一致！**

## 💡 解决方案

### 方案1: 修正 Concat 层索引 (推荐)

问题在于上采样后的特征图尺寸是80x80，应该和P3层(80x80)拼接，而不是P4层(40x40)。

但我们是从P4上采样的，所以第一次上采样后仍应该和P4在**上采样前**拼接。

**正确逻辑**：
- 如果去掉P5，那么最高层是P4(40x40)
- 第一次上采样不应该发生！
- 或者：不要上采样P4，直接用P4作为最大检测层

### 方案2: 完全重新设计 Head

去掉P5后，P4就是最高层，应该直接从P4开始构建Head，不需要从更高层上采样。

