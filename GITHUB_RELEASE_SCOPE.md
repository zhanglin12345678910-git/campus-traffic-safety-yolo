# 本次 GitHub 发布范围纠正

此前首个提交错误地收录了旧 YOLO11 父工程。本次纠正提交以 `campus-safety-agent` 独立项目的当前源码替换仓库内容，保留旧提交作为父提交，不强制改写历史。

## 保留

- `backend/`：FastAPI、LangGraph、数据库迁移、检测与研判工具、测试。
- `frontend/`：Vue 3 界面、依赖锁文件、构建与 Nginx 配置。
- `knowledge/`：校园安全知识材料。
- `deploy/`、Docker Compose：部署代码和脱敏配置模板。
- `scripts/`、`training/`：启动、测试、打包、训练评估代码；不启动训练。
- `docs/`、`models/README.md`：项目、接口、部署、模型配置与测试说明。

## 不发布

真实 `.env`、本机 AI/编辑器配置、数据库、模型权重、原始校园照片、上传图片与视频、运行结果、缓存、日志、审计截图、比赛 Office/PDF 材料、`node_modules` 和构建产物。

前端正在引用的地图、建筑装饰和学校标志保留，并保留素材来源说明；其存在不表示获得学校背书，公开使用仍应核实授权。

当前界面不再引用 `frontend/src/assets/incidents/` 的四张实拍示例，故不发布；部署测试同步检查当前必需界面素材和实拍原图排除规则。

## 运行前提

这是完整源码交付，不是附带生产数据和模型权重的离线安装包。运行前请安装前后端依赖，配置服务与密钥，并按 `models/README.md` 提供权重。已有训练状态和私人巡检记录不会迁移。
