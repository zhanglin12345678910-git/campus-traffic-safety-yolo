# 训练结果图片生成位置说明

## 📊 生成的图片类型


在`runs/train/你的实验名称/`目录下，训练过程中会自动生成以下图片：

### 1. **results.png** - 训练指标曲线图 ⭐
- **位置**：`runs/train/xxx/results.png`
- **生成代码**：`ultralytics/utils/plotting.py` 的 `plot_results()` 函数（第1132行）
- **调用位置**：`ultralytics/models/yolo/detect/train.py` 第138行
- **包含内容**：precision, recall, mAP50, mAP50-95, box_loss, cls_loss等

### 2. **confusion_matrix.png** - 混淆矩阵
- **位置**：`runs/train/xxx/confusion_matrix.png`
- **生成代码**：`ultralytics/utils/metrics.py`
- **调用位置**：`ultralytics/models/yolo/detect/val.py` 第204-207行

### 3. **F1_curve.png, PR_curve.png, P_curve.png, R_curve.png** - 各种曲线
- **位置**：`runs/train/xxx/F1_curve.png` 等
- **生成代码**：`ultralytics/utils/metrics.py`
- **说明**：F1曲线、精确度-召回率曲线、精确度曲线、召回率曲线

### 4. **train_batch0.jpg, train_batch1.jpg** - 训练样本可视化
- **位置**：`runs/train/xxx/train_batch0.jpg`
- **生成代码**：`ultralytics/utils/plotting.py` 的 `plot_images()` 函数（第964行）
- **调用位置**：`ultralytics/models/yolo/detect/train.py` 第124-134行

### 5. **labels.jpg** - 标签分布图
- **位置**：`runs/train/xxx/labels.jpg`
- **生成代码**：`ultralytics/utils/plotting.py` 的 `plot_labels()` 函数
- **调用位置**：`ultralytics/models/yolo/detect/train.py` 第140-144行

---

## 🔍 关键代码位置

### 主要绘图函数

#### 1. `plot_results()` - 生成results.png
```python
# 文件：ultralytics/utils/plotting.py
# 行数：1132-1192
# 功能：读取results.csv，生成训练指标曲线图

def plot_results(file="path/to/results.csv", dir="", segment=False, pose=False, classify=False, on_plot=None):
    """Plot training results from a results CSV file."""
    save_dir = Path(file).parent if file else Path(dir)
    # ... 读取CSV文件
    # ... 绘制曲线
    fig.savefig(save_dir / "results.png", dpi=200)
```

#### 2. `plot_images()` - 生成train_batch图片
```python
# 文件：ultralytics/utils/plotting.py
# 行数：963-1128
# 功能：可视化训练批次中的图像和标签

def plot_images(images, batch_idx, cls, bboxes, ...):
    """Plot images with bounding boxes and labels."""
    # ... 绘制图像
    annotator.im.save(fname)  # save
```

#### 3. 混淆矩阵生成
```python
# 文件：ultralytics/models/yolo/detect/val.py
# 行数：203-207
# 功能：在验证时生成混淆矩阵

if self.args.plots:
    for normalize in True, False:
        self.confusion_matrix.plot(
            save_dir=self.save_dir, names=self.names.values(), 
            normalize=normalize, on_plot=self.on_plot
        )
```

---

## 📁 完整文件路径

### 核心代码文件：

1. **绘图函数库**
   - `ultralytics/utils/plotting.py` - 所有绘图函数
   - `ultralytics/utils/metrics.py` - 混淆矩阵等

2. **训练时调用**
   - `ultralytics/models/yolo/detect/train.py` - 目标检测训练器
   - `ultralytics/models/yolo/segment/train.py` - 分割训练器
   - `ultralytics/models/yolo/pose/train.py` - 姿态估计训练器

3. **验证时调用**
   - `ultralytics/models/yolo/detect/val.py` - 目标检测验证器

---

## 🎯 生成的时机

### results.png 生成时机：
- 每个epoch结束后自动生成
- 代码位置：`ultralytics/models/yolo/detect/train.py` 第136-138行

```python
def plot_metrics(self):
    """Plots metrics from a CSV file."""
    plot_results(file=self.csv, on_plot=self.on_plot)  # save results.png
```

### train_batch图片生成时机：
- 前几个epoch开始时生成
- 代码位置：`ultralytics/models/yolo/detect/train.py` 第124-134行

### confusion_matrix生成时机：
- 验证完成时生成
- 代码位置：`ultralytics/models/yolo/detect/val.py` 第203-207行

---

## 🛠️ 自定义绘图脚本

### 项目根目录的 `plot_result.py`

这是一个自定义的绘图脚本，可以用来对比多个实验的结果。

**使用方式：**
```python
# 修改 plot_result.py 中的names列表
names = ['实验1', '实验2', '实验3']

# 运行
python plot_result.py
```

**生成的图片：**
- `metrice_curve.png` - 指标对比图
- `loss_curve.png` - 损失对比图

---

## 📝 如何查看具体代码

### 查看results.png生成代码：
```bash
# 在ultralytics/utils/plotting.py第1132行开始
```

### 查看train_batch图片生成代码：
```bash
# 在ultralytics/utils/plotting.py第964行开始
```

### 查看confusion_matrix生成代码：
```bash
# 在ultralytics/models/yolo/detect/val.py第203-207行
```

---

## 🎓 总结

**主要绘图函数和位置：**

| 图片 | 代码文件 | 函数 | 行数 |
|------|---------|------|------|
| `results.png` | `ultralytics/utils/plotting.py` | `plot_results()` | 1132-1192 |
| `train_batchX.jpg` | `ultralytics/utils/plotting.py` | `plot_images()` | 963-1128 |
| `confusion_matrix.png` | `ultralytics/utils/metrics.py` | `plot()` | - |
| `labels.jpg` | `ultralytics/utils/plotting.py` | `plot_labels()` | - |
| 各种curves | `ultralytics/utils/metrics.py` | - | - |

**训练时自动调用位置：**

| 操作 | 代码文件 | 函数 | 行数 |
|------|---------|------|------|
| 训练指标图 | `ultralytics/models/yolo/detect/train.py` | `plot_metrics()` | 136-138 |
| 训练样本图 | `ultralytics/models/yolo/detect/train.py` | `plot_training_samples()` | 124-134 |
| 混淆矩阵 | `ultralytics/models/yolo/detect/val.py` | `print_results()` | 203-207 |

